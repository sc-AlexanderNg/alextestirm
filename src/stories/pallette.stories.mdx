import { Meta, Canvas, Title } from '@storybook/blocks';

export const colors = [
  {
    Primary: [
      { '--color-im-dark-blue': '#14477d' },
      { '--color-im-mid-blue': '#1b75bc' },
      { '--color-im-light-blue': '#6dcff6' },
      { '--color-im-grey': '#58595b' },
    ],
  },
  {
    Secondary: [
      { '--color-im-teal': '#00a88e' },
      { '--color-im-yellow': '#ffdd00' },
      { '--color-im-orange': '#f7931e' },
      { '--color-im-purple': '#8a0d71' },
      { '--color-im-red': '#ed1c24' },
      { '--color-im-green': '#8dc13f' },
    ],
  },
  {
    Web: [
      { '--color-white': '#fff' },
      { '--color-black': '#000' },
      { '--color-background-1': '#f7f7fe' },
      { '--color-background-2': '#ebf0f8' },
      { '--color-background-3': '#023459' },
      { '--color-light-gray': '#cccccc' },
      { '--color-light-grey': '#cccccc' },
      { '--color-dark-text': '#353637' },
      {
        '--color-teal-gradient':
          'linear-gradient(180deg, #6dcff6 0%, #00a88e 100%)',
      },
      {
        '--color-purple-gradient':
          'linear-gradient(180deg, #a2115e 0%, #8a0d71 100%)',
      },
      {
        '--color-orange-gradient':
          'linear-gradient(180deg, #f7931e 0%, #f4751f 100%)',
      },
      {
        '--color-light-blue-gradient':
          'linear-gradient(180deg, #6dcff6 0%, #1b75bc 100%)',
      },
      {
        '--color-dark-blue-gradient':
          'linear-gradient(180deg, #1b75bc 0%, #14477d 100%)',
      },
      {
        '--color-darker-blue-gradient':
          'linear-gradient(180deg, #235e8b 0%, #0a3b6b 100%)',
      },
    ],
  },
];

<Meta title="Design/Colors" />

<Title>Colors</Title>

<div className="colors">
  {colors.map((category, index) => {
    return (
      <div key={index}>
        <h2>{Object.keys(category)[0]}</h2>
        <ul>
          {Object.values(category)[0].map((color, index) => {
            return (
              <li key={index} style={{ '--color': Object.values(color)[0] }}>
                <b>{Object.keys(color)[0]}</b>
                <br />
                {Object.values(color)[0]}
              </li>
            );
          })}
        </ul>
      </div>
    );
  })}
</div>
