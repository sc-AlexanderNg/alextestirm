import styles from './MainNavigation.module.css';

const MainNavigation = (): JSX.Element => {
  return <div className={styles['main-navigation']}>Navigation</div>;
};

export default MainNavigation;
